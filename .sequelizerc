// # -> Arq. de config do sequelize
const {resolve} = require('path'); // Importa o resolve do express, para dir.
// Configs
module.exports = {
    config: resolve(__dirname, 'src', 'config', 'database.js'), // Define o dir. do arq. de config da db
    'models-path': resolve(__dirname, 'src', 'models'), // Define o dir. dos models
    'migrations-path': resolve(__dirname, 'src', 'database', 'migrations'), // Define o dir. das migrations
    'seeders-path': resolve(__dirname, 'src', 'database', 'seeders'), // Define o dir. das seeders
};